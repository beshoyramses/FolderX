/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['firebasestorage.googleapis.com','api.dicebear.com',"avatar.iran.liara.run"],
    },
  };
  
  export default nextConfig;
  